import { css } from '@emotion/react';

const reset = css`
  * {
    padding: 0;
    margin: 0;
  }

  li {
    list-style: none;
  }

  button {
    border: none;
    background: none;
  }

  em {
    font-style: normal;
  }
`;

export default reset;
